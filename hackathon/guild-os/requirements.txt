# GuildOS — Python dependencies
# Install: pip install -r requirements.txt

# Agent protocol
a2a-sdk[http-server]>=1.0.0

# Blockchain interaction
web3>=6.0.0

# HTTP client (for 8004scan API, AgentFightClub ClawBank)
httpx>=0.27.0

# Configuration
python-dotenv>=1.0.0

# CLI interaction (human gates)
click>=8.0.0

# Data validation
pydantic>=2.0.0

# Cryptography (SHA-256, signing)
eth-account>=0.11.0

# ASGI server (SpecialistAgent A2A HTTP server)
uvicorn>=0.30.0

# Web framework (A2A route mounting)
fastapi>=0.111.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Linting
ruff>=0.4.0
