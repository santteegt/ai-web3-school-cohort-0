name: Deploy (placeholder)

# GuildOS is a local demo application — there is no cloud deployment for the hackathon.
# This workflow is a placeholder for post-hackathon productionization.

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — add deployment steps here post-hackathon
        run: echo "Deployment not configured for hackathon build. See README for run instructions."
